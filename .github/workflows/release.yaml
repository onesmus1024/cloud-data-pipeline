#on:
#  push:
#    tags:
#      - "v*"

on: create

permissions:
  contents: write
name: Release
jobs:
  release:
    name: Release GitHub Actions
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/release-github-actions@v6